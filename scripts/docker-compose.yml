version: '3.8'

services:
  k6:
    image: grafana/k6:latest
    container_name: k6
    volumes:
      - ./k6:/k6
    entrypoint: ['k6']
    working_dir: /k6
    networks:
      - ride-sharing-net

networks:
  ride-sharing-net:
    external: true
    name: uit-go-project_ride-sharing-net
